spring:
  application:
    name: project-service
  data:
    mongodb:
      host: localhost
      database: projects
      port: 27017

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

ribbon:
  eureka:
    enabled: true

server:
  port: 4000